# 智能配餐系统 — Design Context

## Design Context

### Users

- Primary users are Chinese-speaking people who care about eating well but may not know what diet fits their body condition, goals, or budget.
- The product supports multiple real-life scenarios: daily meal decisions, fitness and fat-loss planning, household grocery planning, and special health-management needs.
- Typical usage happens on mobile during short planning windows such as deciding what to eat today, planning a grocery run, or checking whether a plan still fits current constraints.
- Primary job to be done: users describe their body condition, dietary goals, constraints, and spending level, then quickly receive a practical and trustworthy meal plan without needing nutrition expertise.

### Usage Context

- Shortest successful path: `登录/注册 -> 资料补全 -> 对话 intake -> 查看结果 -> 继续/复用`.
- First-time users start with minimal personal data; sensitive body metrics are requested only when the planner needs them.
- Returning users immediately see whether they can continue, reuse a prior setup, or fix a missing prerequisite.

### Brand Personality

- Professional, trustworthy, caring.
- The interface feels efficient and expert-led, while giving users warmth and guidance — supported, not judged.
- Copy is grounded and useful: explain what matters, what the system knows, and what the user should do next.
- Three-word personality: **refined, reliable, warm**.

### Aesthetic Direction

- Premium minimal. Think Notion, Linear, Apple Notes — quiet confidence, no decoration.
- Light-toned, mobile-first, with a grounded consumer-product feel rather than a dashboard or AI-demo feel.
- Refined neutral surfaces with a single restrained accent. No green-heavy wellness branding.
- Visual emphasis comes from hierarchy, spacing, typography, and state clarity — not novelty effects or decorative chrome.
- Both light and dark mode are intentional. Light mode is primary and polished; dark mode is equally considered.

### Aha Moment

The product becomes convincing when a user sees that the system understands their goal, budget, and missing inputs, then turns that into a concrete budget-safe daily plan with an obvious next step.

### Anti-References

- NOT an AI demo, speculative startup landing page, or dashboard of equal-priority cards.
- NOT generic admin systems (Element Plus defaults), loud wellness branding, or overly saturated health-app palettes.
- NO fake affordances, hover-only actions, decorative jargon, or "Powered by AI" framing on primary surfaces.
- NO gradient text, glassmorphism, neon accents on dark backgrounds, cyan-on-dark, or purple-to-blue gradients.
- NO identical card grids, hero metric layouts, or icon-above-every-heading templates.
- NO pure black (#000) or pure white (#fff) on large surfaces — always tinted warm.
- NO bounce or elastic easing — they feel dated.

---

## Design System

### Color Palette

Built with oklch for perceptual uniformity. All colors tinted warm (hue 75).

**Light Mode:**

| Token | Value | Role |
|-------|-------|------|
| `--color-bg-canvas` | `oklch(97.5% 0.006 75)` | Page background |
| `--color-bg-subtle` | `oklch(96% 0.007 75)` | Subtle background variation |
| `--color-bg-elevated` | `oklch(99% 0.004 75 / 0.92)` | Topbar, elevated surfaces |
| `--color-surface` | `oklch(98.5% 0.004 75)` | Default surface |
| `--color-surface-raised` | `oklch(100% 0 0)` | Cards, raised elements |
| `--color-surface-muted` | `oklch(96.5% 0.006 75)` | Muted backgrounds |
| `--color-surface-emphasis` | `oklch(18% 0.012 75)` | Dark emphasis panels |
| `--color-border-soft` | `oklch(25% 0.01 75 / 0.1)` | Default borders |
| `--color-border-strong` | `oklch(25% 0.01 75 / 0.18)` | Emphasized borders |
| `--color-border-accent` | `oklch(55% 0.08 75 / 0.2)` | Accent-tinted borders |
| `--color-accent` | `oklch(62% 0.14 75)` | Primary accent (warm amber) |
| `--color-accent-strong` | `oklch(48% 0.12 70)` | Strong accent (buttons, CTAs) |
| `--color-accent-soft` | `oklch(62% 0.14 75 / 0.1)` | Soft accent background |
| `--color-text-main` | `oklch(16% 0.012 75)` | Primary text (warm near-black) |
| `--color-text-secondary` | `oklch(42% 0.015 75)` | Secondary text |
| `--color-text-light` | `oklch(50% 0.012 75)` | Meta text (WCAG AA compliant) |
| `--color-danger` | `oklch(50% 0.16 20)` | Error/danger |
| `--color-warning` | `oklch(65% 0.16 65)` | Warning |

**Dark Mode:** Warm dark neutrals (hue 75), lighter accent at `oklch(72% 0.12 75)`. Full token set in `main.css`.

**Accent usage rules:**
- Accent appears ONLY on interactive elements (buttons, links, focus rings) and key CTAs.
- Never on decorative surfaces, shadows, borders, or backgrounds.
- One accent color for the entire UI — no secondary accent colors.

### Typography

**Font stack:** `"DM Sans", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif`

DM Sans loaded from Google Fonts (weights 400, 500, 700) with `display=swap`. CJK fallback to system fonts.

**Type scale (fixed rem for app UI):**

| Token | Size | Role |
|-------|------|------|
| `--text-xs` | `0.75rem` (12px) | Captions, badges |
| `--text-sm` | `0.8125rem` (13px) | Labels, meta text |
| `--text-base` | `0.9375rem` (15px) | Body text (default) |
| `--text-lg` | `1.0625rem` (17px) | Subheadings |
| `--text-xl` | `1.1875rem` (19px) | Card headings |
| `--text-2xl` | `clamp(1.5rem, 3vw, 1.75rem)` | Section headings |
| `--text-3xl` | `clamp(1.75rem, 3.5vw, 2.25rem)` | Page headings |
| `--text-4xl` | `clamp(2rem, 4vw, 2.75rem)` | Hero headings |

**Weight tokens:**

| Token | Value | Role |
|-------|-------|------|
| `--weight-regular` | 400 | Body text |
| `--weight-medium` | 500 | Labels, nav items, emphasis |
| `--weight-bold` | 700 | Headings, CTAs, key data |

**Line-height tokens:**

| Token | Value | Role |
|-------|-------|------|
| `--leading-tight` | 1.15 | Large headings |
| `--leading-snug` | 1.35 | Subheadings |
| `--leading-normal` | 1.55 | Body text (default) |
| `--leading-relaxed` | 1.7 | Long-form reading |

**Letter-spacing tokens:**

| Token | Value | Role |
|-------|-------|------|
| `--tracking-tight` | -0.01em | Large headings |
| `--tracking-normal` | 0 | Body text |
| `--tracking-wide` | 0.04em | Labels, buttons |
| `--tracking-wider` | 0.08em | Uppercase eyebrow labels |

### Spacing

| Token | Value |
|-------|-------|
| `--space-2` | 8px |
| `--space-3` | 12px |
| `--space-4` | 16px |
| `--space-5` | 20px |
| `--space-6` | 24px |
| `--space-8` | 32px |

### Shadows

Minimal, warm-tinted. Cards have NO shadow by default; subtle shadow appears on hover.

| Token | Value |
|-------|-------|
| `--shadow-sm` | `0 2px 8px oklch(16% 0.01 75 / 0.06)` |
| `--shadow-md` | `0 4px 16px oklch(16% 0.01 75 / 0.08)` |
| `--shadow-lg` | `0 8px 32px oklch(16% 0.01 75 / 0.12)` |

### Border Radius

| Context | Radius |
|---------|--------|
| Large panels (chat topbar, overlays) | 16px |
| Cards, auth forms, day cards | 12-14px |
| Buttons, inputs, small cards | 10px |
| Tags, badges, pills | 6-8px |
| Fully round (status pills, avatar) | 999px |

### Component Patterns

**Buttons** (`el-button` overrides in `main.css`):
- 10px border-radius, medium weight, accent-colored primary
- Active state: `scale(0.97)` feedback
- Hover: subtle background shift, no translateY
- Large: 48px min-height

**Cards** (`el-card` overrides in `main.css`):
- 16px border-radius, no shadow by default
- Hover: subtle shadow-sm appears
- Background: `var(--color-surface-raised)` (flat, no gradients)
- Border: 1px solid `var(--color-border-soft)`

**Inputs** (`el-input` overrides in `main.css`):
- 10px border-radius
- Focus: 2px accent-colored inset ring
- Hover: border darkens to `--color-border-strong`

**Navigation** (App.vue):
- Topbar: 60px height, sticky, blurred background
- Brand: 36px logo icon + brand text (no subtitle)
- Nav items: 40px height, 10px radius, medium weight, text-sm
- Mobile: bottom sticky nav, 48px items, 5-column grid
- Breakpoint: 720px (desktop nav hides, mobile nav appears)

**Home Page** (HomeView.vue):
- Single-column flow, max-width 640px
- ONE primary CTA ("Start Planning")
- Profile completion: inline banner (conditional, only when incomplete)
- Quick links: 4-column grid of icon+label tiles

**Chat Interface** (MealPlanView.vue):
- Two-column: chat panel (1.7fr) + context rail (0.88fr)
- Collapses to single column at 960px
- User bubbles: solid accent background
- Assistant bubbles: muted surface background
- Full-screen overlays for generation progress and results

**Recipes** (RecipesView.vue):
- Sidebar filters (sticky on desktop, inline on mobile)
- Grid of recipe cards with emoji placeholders
- Detail dialog for full recipe view

### Motion

- Transitions: 160ms ease for state changes
- Route transitions: 0.2s opacity fade
- Overlay transitions: `cubic-bezier(0.22, 1, 0.36, 1)`
- No bounce, no elastic easing
- `prefers-reduced-motion`: overlay durations collapse to 1ms

### Responsive Breakpoints

| Breakpoint | Behavior |
|------------|----------|
| 960px | Two-column layouts collapse to single column |
| 720px | Desktop nav hides, mobile bottom nav appears |
| 640px | Buttons go full-width, forms stack vertically |
| 560px | Topbar simplifies |

All interactive elements maintain 44px minimum touch targets on mobile.

---

## Design Principles

1. **Truth before flourish**: every CTA and status must match real behavior.
2. **One dominant next action**: each screen makes the next useful step obvious within seconds.
3. **Explain missing information**: tell users what is missing, why it matters, and how to resolve it.
4. **Professional clarity over wellness cliche**: elegant, product-grade visual language — not health-app green or generic dashboard styling.
5. **Lower cognitive load**: remove redundant choices, duplicate actions, and decorative framing that does not help a real user finish a meal-planning task.
6. **Mobile-safe by default**: no critical hover behavior, no horizontal overflow, touch targets sized for phones.
7. **Design both themes on purpose**: every major surface has a considered light and dark treatment.
8. **Restrained accent**: one accent color, used only on interactive elements — never on decorative surfaces.
9. **Hierarchy through typography and spacing**: not through color saturation, gradients, or shadows.

---

## Technical Context

### Stack

- **Framework**: Vue 3.4+ (Composition API, `<script setup lang="ts">`)
- **Build**: Vite 5
- **UI Library**: Element Plus 2.4+ (heavily customized via CSS overrides)
- **State**: Pinia 2.1+
- **Routing**: Vue Router 4.2+
- **i18n**: vue-i18n 12+ (zh default, en fallback)
- **HTTP**: Axios 1.6+
- **Testing**: Vitest 4.1+

### Key Files

| File | Purpose |
|------|---------|
| `frontend/src/assets/main.css` | All design tokens, CSS reset, Element Plus overrides, dark mode |
| `frontend/src/App.vue` | Layout shell, topbar, navigation, mobile nav |
| `frontend/src/views/HomeView.vue` | Dashboard — single dominant CTA + quick links |
| `frontend/src/views/MealPlanView.vue` | Chat-based meal planning (core feature) |
| `frontend/src/views/RecipesView.vue` | Recipe browsing with filters |
| `frontend/src/views/WeeklyPlanView.vue` | Weekly plan management |
| `frontend/src/views/ProfileView.vue` | User profile with onboarding mode |
| `frontend/src/views/ShoppingListView.vue` | Shopping list management |
| `frontend/src/views/HistoryView.vue` | Meal plan history |
| `frontend/src/views/LoginView.vue` | Login form |
| `frontend/src/views/RegisterView.vue` | Registration form |
| `frontend/src/components/meal-chat/` | Chat overlays and status panel |
| `frontend/src/components/common/AppEmptyState.vue` | Reusable empty state |
| `frontend/src/stores/` | Pinia stores (auth, user, shopping, weeklyPlan) |
| `frontend/src/api/index.ts` | Axios client and all API interfaces |
| `frontend/src/locales/zh.json` | Chinese translations |
| `frontend/src/locales/en.json` | English translations |

### Routes

| Path | Auth | Description |
|------|------|-------------|
| `/login` | No | Login form |
| `/register` | No | Registration form |
| `/` | Yes | Home dashboard |
| `/meal-plan` | Yes | Chat-based meal planning |
| `/weekly-plan` | Yes | Weekly plan management |
| `/recipes` | Yes | Recipe browsing |
| `/shopping-list` | Yes | Shopping list |
| `/profile` | Yes | User profile |
| `/history` | Yes | Meal plan history |
